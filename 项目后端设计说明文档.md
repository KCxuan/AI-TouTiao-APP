# AI掘金头条新闻系统 (Toutiao News)

一个基于 FastAPI 和 SQLAlchemy 构建的新闻系统，支持用户注册登录、新闻浏览、新闻发布与管理、收藏和历史记录，并集成 Redis 缓存。系统同时提供后端驱动的 AI 能力：普通对话、自动模式分流，以及带人工审核的深度新闻研究 Agent。

## 1-1 项目概述

这是一个仿今日头条的新闻系统，采用前后端分离架构。后端使用异步 Python 框架 FastAPI 开发，使用 MySQL 作为数据库存储，通过 SQLAlchemy ORM 进行数据访问，Redis 作为缓存层。前端为独立的 Vue 3 单页应用（`frontend` 目录）。

新闻业务提供完整的用户管理、新闻浏览与创作（发布/编辑/删除）、收藏和浏览历史。AI 能力不再由浏览器直连模型，而是由 FastAPI 调用 LLM 与 LangGraph 研究图：普通对话走 Chat Service；涉及近期新闻、多来源对比或可追溯证据时走 Research Agent；自动模式由分类器决定进入对话、研究或先向用户澄清。

接口字段、请求示例和错误行为以 `API接口规范文档.md` 为准。`NewsPilot新闻研究Agent架构与实现指南.md` 是后续演进设计稿，不能当作当前仓库的已有功能。其中 SSE、收藏/历史作为 Agent 工具、站外正文抓取等尚未实现；AI 登录鉴权、研究任务持久化、站内新闻检索已经落地。

## 1-2 技术栈

- **后端框架**: FastAPI
- **数据库**: MySQL
- **ORM**: SQLAlchemy (异步)
- **数据库驱动**: aiomysql
- **密码加密**: passlib + bcrypt
- **缓存系统**: Redis (redis-py asyncio)
- **异步支持**: Python asyncio（新闻/用户/收藏/历史接口）
- **前端框架**: Vue 3 (Composition API) + Vite + Vue Router + Pinia + Axios
- **AI 编排**: LangGraph（研究图、人工审核 interrupt、SQLite Checkpointer）
- **LLM 调用**: LangChain `ChatOpenAI`（OpenAI 兼容接口，模型与密钥由项目根目录 `.env` 配置）
- **新闻搜索**: 站内 MySQL 关键词检索 + Tavily 站外新闻搜索（另有 DDGS / Fake 实现未启用）

## 1-3 项目结构

```
app_toutiao/                              # 项目根目录
├── cache/                              # 缓存业务层（按键名封装读写与失效）
│   └── news_cache.py                  # 新闻缓存：分类/列表/详情/相关新闻的存取与清理
│
├── config/                             # 配置相关
│   ├── db_config.py                   # 数据库配置（异步引擎与会话依赖）
│   └── cache_conf.py                  # Redis 连接与缓存通用原语（get/set/delete/pattern删）
│
├── crud/                               # 数据访问层（CRUD 操作，缓存读写下沉于此层）
│   ├── favorite.py                    # 收藏相关数据库操作
│   ├── history.py                     # 历史记录相关数据库操作
│   ├── news.py                        # 新闻相关数据库操作（含缓存与关键词搜索）
│   ├── research.py                    # 研究任务 research_run 读写
│   └── users.py                       # 用户相关数据库操作
│
├── models/                             # 数据模型定义
│   ├── favorite.py                    # 收藏数据模型
│   ├── history.py                     # 历史记录数据模型
│   ├── news.py                        # 新闻与分类数据模型
│   ├── research.py                    # 研究任务 research_run 模型
│   └── users.py                       # 用户与令牌数据模型
│
├── routers/                            # API 路由定义
│   ├── favorite.py                    # 收藏相关 API 路由
│   ├── history.py                     # 历史记录相关 API 路由
│   ├── news.py                        # 新闻相关 API 路由（浏览/发布/编辑/删除）
│   ├── users.py                       # 用户相关 API 路由
│   ├── chat.py                        # 普通 AI 对话 API
│   ├── auto.py                        # 自动模式分流 API
│   └── research.py                    # 深度研究启动与审核 API
│
├── schemas/                            # 数据验证模型（Pydantic 模型）
│   ├── base.py                        # 新闻条目公共字段模型
│   ├── favorite.py                    # 收藏数据验证模型
│   ├── history.py                     # 历史记录数据验证模型
│   ├── news.py                        # 新闻发布/编辑请求模型
│   ├── users.py                       # 用户数据验证模型
│   ├── chat.py                        # 普通对话请求/结果模型
│   ├── auto.py                        # 自动模式请求/分流结果模型
│   └── research.py                    # 研究启动、审核与结果模型
│
├── services/                           # AI 业务服务层（Router 不直接操作图或模型）
│   ├── chat_service.py                # 普通对话：组装消息并调用 LLM
│   ├── auto_service.py                # 自动模式：分类后转交 Chat 或 Research
│   └── research_service.py            # 研究任务：启动图、恢复审核、封装结果
│
├── new_research_agent/                 # 新闻研究 Agent（LangGraph）
│   ├── llm.py                         # 共享 LLM 配置（读取 .env）
│   ├── agentstate.py                  # 研究图共享状态
│   ├── research_planner.py            # 研究计划与证据审查的结构化输出
│   ├── search_types.py                # 搜索结果标准结构
│   ├── search_tool.py                 # 站外新闻搜索工具（当前启用 Tavily）
│   ├── internal_search.py             # 站内新闻搜索（复用 crud.news 关键词查询）
│   ├── nodes.py                       # 规划、搜索、评估、写稿、审核、定稿节点
│   ├── routes.py                      # 证据评估后与审核后的条件路由
│   ├── graph.py                       # 研究图编译（SqliteSaver，库文件在 data/）
│   └── main.py                        # 命令行调试入口，也可作为图的初始状态工厂
│
├── utils/                              # 工具函数目录
│   ├── auth.py                        # 基于 token 的用户认证依赖
│   ├── exception.py                   # 统一异常响应
│   ├── exception_handlers.py          # 全局异常处理器注册
│   ├── response.py                    # 统一成功响应封装
│   └── security.py                    # 密码加密与校验
│
├── frontend/                           # 前端项目（Vue 3 + Vite，独立运行于 5173 端口）
│   └── src/
│       ├── api/                       # 接口封装（axios 拦截器 + 各模块 API）
│       ├── stores/                    # Pinia 状态管理（用户登录态）
│       ├── router/                    # 路由与登录守卫
│       ├── views/                     # 页面（登录/列表/详情/发布/我的发布/收藏/历史/个人中心/AI 研究）
│       ├── composables/               # 组合式函数（全局 toast）
│       └── utils/                     # 工具函数（时间格式化、字段兼容）
│
├── main.py                             # 应用入口文件
├── database.sql                        # 数据库初始化脚本
├── requirements.txt                    # Python 依赖（当前以 AI/LangGraph 相关包为主）
├── API接口规范文档.md                    # 当前已挂载 HTTP API 的接口规范
├── NewsPilot新闻研究Agent架构与实现指南.md  # 研究 Agent 后续演进设计稿
└── 项目后端设计说明文档.md                # 本文档
```

## 1-4 功能模块

### 1. 用户管理模块

- 用户注册
- 用户登录
- 用户信息获取
- 用户信息更新
- 用户密码修改

### 2. 新闻模块

- 新闻分类获取
- 新闻列表获取（支持分页和分类筛选，按发布时间倒序）
- 新闻关键词搜索（匹配标题和简介，标题命中优先，不走缓存；人与 Agent 共用同一查询）
- 新闻详情获取（含浏览量统计与相关新闻推荐）
- 发布新闻（登录用户，作者与归属由服务端填充）
- 获取我的发布列表（仅本人文章，按发布时间倒序，不缓存）
- 编辑新闻（仅本人文章，部分更新：只修改传入字段）
- 删除新闻（仅本人文章，级联清理对应收藏与浏览记录）

### 3. 收藏模块

- 添加收藏
- 取消收藏
- 收藏列表获取
- 清空所有收藏
- 检查收藏状态

### 4. 浏览历史模块

- 添加浏览记录（同一新闻重复浏览仅更新时间）
- 浏览历史列表获取（含 historyId，删除时需使用）
- 删除单条浏览记录
- 清空浏览历史

### 5. 缓存模块

- 新闻详情缓存（读路径 + 写后失效）
- 新闻列表缓存（读路径 + 写后失效）
- 分类数据缓存（读路径）
- 相关新闻缓存（读路径 + 写后失效）
- 写操作的缓存失效矩阵与 commit 后失效时序控制

### 6. AI 助手模块（后端实现）

当前由 FastAPI 同步接口提供，前端只负责发请求和展示结果，不保存模型密钥，也不直连模型供应商。

- **普通对话**：根据短期历史生成回复，最多使用最近 20 条历史；不调用新闻搜索工具
- **自动模式**：由 LLM 将用户消息分类为 `chat`、`research` 或 `clarify`；研究分支由 Router 调用同一套 `start_research_for_user`，以便写入任务表
- **深度研究**：LangGraph 规划研究目标，先按实体关键词检索站内新闻，不足再用 Tavily 补齐；评估证据并生成带来源编号的报告草稿
- **人工审核**：草稿生成后暂停，用户可选择通过、修改、补查或调整研究目标
- **任务恢复**：同一用户优先恢复待审核草稿，没有待审核时恢复最近一份已完成报告；新研究开始前会放弃旧的待审核任务。前端 `/ai` 需登录，进入页面时请求 `GET /current`；「清空记录」请求 `DELETE /current`
- **引用校验**：报告正文必须引用真实存在的来源编号，程序再追加带 URL 的引用来源目录

当前限制：

- 全部 AI 接口需要登录
- 请求为同步 JSON，不使用 SSE 或 WebSocket
- LangGraph 检查点保存在 `data/research_checkpoints.db`（SQLite，该目录已加入 `.gitignore`）；任务元数据写入 MySQL `research_run`
- 对话记录仍不写入 `ai_chat` 表
- 研究证据来自站内标题/简介摘要和 Tavily 新闻摘要，不抓取站外正文
- 收藏和浏览历史不进入研究 Agent（与研究主题耦合弱，当前 DEMO 明确不做）

## 1-5 数据库设计

### 主要数据表

1. **用户表 (user)**
    - 用户基本信息存储
    - 包含用户名、密码(加密)、昵称、头像等字段

2. **用户令牌表 (user_token)**
    - 用户认证令牌管理
    - 支持令牌过期机制
    - 同一用户重新登录时替换原 token；项目暂未提供 refresh token 和退出登录接口

3. **新闻分类表 (news_category)**
    - 新闻分类信息

4. **新闻表 (news)**
    - 新闻内容存储
    - 包含标题、内容、作者、浏览量、发布时间等字段
    - `user_id` 字段标识发布者（外键 user.id，ON DELETE SET NULL）；
      存量旧数据该字段为 NULL，视为"无主文章"，仅可浏览不可编辑/删除

5. **收藏表 (favorite)**
    - 用户收藏记录
    - 关联用户和新闻（ON DELETE CASCADE：新闻删除时收藏自动清理）
    - (user_id, news_id) 唯一约束

6. **浏览历史表 (history)**
    - 用户浏览历史记录
    - 关联用户和新闻（ON DELETE CASCADE：新闻删除时记录自动清理）

7. **AI 聊天记录表 (ai_chat)**
    - 预留的 AI 对话持久化表
    - 当前 Chat / Auto / Research 均未写入该表

8. **相关新闻表 (related_news)**
    - 预留的文章关联表（当前相关新闻由查询动态计算，暂未使用）

9. **研究任务表 (research_run)**
    - 记录每个登录用户的研究任务元数据，用于恢复和权限校验
    - `thread_id` 对应 LangGraph 检查点；`user_id` 外键 `user.id`
    - `status` 为 ENUM：`waiting_review` / `completed` / `abandoned`
    - 清空记录或开始新研究时对可见任务做软删除（改为 `abandoned`），行仍保留
    - `database.sql` 若尚未包含该表，需按模型 `models/research.py` 自行建表

## 1-6 缓存设计

### 缓存策略

系统采用 Redis 作为缓存层（Cache-Aside 旁路缓存模式），对高频访问的公共数据
进行缓存。缓存逻辑下沉至 CRUD 层：路由代码不感知缓存，缓存命中与未命中返回
的 JSON 完全一致（通过 jsonable_encoder 序列化 ORM 对象写入，读取后以
News(**item) 重建 ORM 实例）。

### 缓存数据类型

1. **新闻分类缓存**
    - 缓存键: `news:categories`

2. **新闻列表缓存**
    - 缓存键: `news_list:{category_id}:{page}:{size}`

3. **新闻详情缓存**
    - 缓存键: `news_detail:{news_id}`

4. **相关新闻缓存**
    - 缓存键: `related_news:{news_id}:{category_id}:{limit}`

以上新闻缓存过期时间统一为 7200 秒（2 小时），由 `cache/news_cache.py` 调用
`set_cache` 时传入，作为失效机制的兜底。

### 缓存失效机制（写后失效）

数据写操作在数据库事务 **commit 之后** 执行缓存清理（避免并发读请求在提交前
回填旧数据），失效矩阵如下：

| 写操作 | news_detail:{id} | news_list | related_news | news:categories |
|--------|------------------|-----------|--------------|-----------------|
| 发布新闻 | 无需（新 id 无缓存） | 清该分类所有页 | 清同分类相关缓存 | 无需 |
| 编辑新闻 | 精准删除 | 清该分类（跨分类编辑时新旧分类均清） | 全量删除 | 无需 |
| 删除新闻 | 精准删除 | 清原分类所有页 | 全量删除 | 无需 |

相关新闻缓存需要全量删除的原因：其键以"其他文章 id"开头，无法反查哪些推荐
列表包含被修改的文章。

### 缓存配置

在 cache_conf.py 中配置 Redis 连接信息：

```python
REDIS_HOST = "localhost"
REDIS_PORT = 6379
REDIS_DB = 0
```

底层提供两类删除原语：`delete_cache(*keys)`（精准删除）与
`delete_cache_pattern(pattern)`（基于 scan_iter 的模式删除，避免 KEYS 命令
阻塞 Redis）。业务键规则统一封装在 cache/news_cache.py 中。

### 不缓存的数据

- 用户"我的发布"列表：每人内容不同、随增删实时变化，不适合共享缓存
- 新闻总数 count 查询：直接查库，保证分页 total 的实时性
- 新闻关键词搜索：直接查库，不走列表缓存，避免搜索结果与缓存页混淆
- AI 对话、自动分流和研究任务：不经过 Redis；图状态在 SQLite Checkpointer，任务索引在 MySQL `research_run`

## 1-7 API 接口说明

### 用户相关接口

| 接口                 | 方法 | 说明         |
| -------------------- | ---- | ------------ |
| `/api/user/register` | POST | 用户注册     |
| `/api/user/login`    | POST | 用户登录     |
| `/api/user/info`     | GET  | 获取用户信息 |
| `/api/user/update`   | PUT  | 更新用户信息 |
| `/api/user/password` | PUT  | 修改用户密码 |

### 新闻相关接口

| 接口                          | 方法   | 说明                     |
| ----------------------------- | ------ | ------------------------ |
| `/api/news/categories`        | GET    | 获取新闻分类列表         |
| `/api/news/list`              | GET    | 获取新闻列表（时间倒序） |
| `/api/news/search`            | GET    | 关键词搜索新闻（标题/简介） |
| `/api/news/detail`            | GET    | 获取新闻详情             |
| `/api/news/publish`           | POST   | 发布新闻（需登录）       |
| `/api/news/mine`              | GET    | 获取我的发布列表（登录） |
| `/api/news/update`            | PUT    | 编辑自己的新闻（登录）   |
| `/api/news/delete/{news_id}`  | DELETE | 删除自己的新闻（登录）   |

### 收藏相关接口

| 接口                   | 方法   | 说明             |
| ---------------------- | ------ | ---------------- |
| `/api/favorite/check`  | GET    | 检查新闻收藏状态 |
| `/api/favorite/add`    | POST   | 添加收藏         |
| `/api/favorite/remove` | DELETE | 取消收藏         |
| `/api/favorite/list`   | GET    | 获取收藏列表     |
| `/api/favorite/clear`  | DELETE | 清空所有收藏     |

### 浏览历史相关接口

| 接口                               | 方法   | 说明                         |
| ---------------------------------- | ------ | ---------------------------- |
| `/api/history/add`                 | POST   | 添加浏览记录                 |
| `/api/history/list`                | GET    | 获取浏览历史列表（含 historyId） |
| `/api/history/delete/{history_id}` | DELETE | 删除单条浏览记录（用 historyId） |
| `/api/history/clear`               | DELETE | 清空浏览历史                 |

### AI 相关接口

| 接口                                      | 方法 | 说明                                   |
| ----------------------------------------- | ---- | -------------------------------------- |
| `/api/ai/chat`                            | POST   | 普通对话（需登录）                     |
| `/api/ai/auto`                            | POST   | 自动判断进入对话、研究或澄清（需登录） |
| `/api/ai/research/start`                  | POST   | 发起深度研究，通常返回待审核草稿       |
| `/api/ai/research/{thread_id}/review`     | POST   | 提交审核结果并恢复同一研究任务         |
| `/api/ai/research/current`                | GET    | 恢复当前用户待审核或最近已完成的研究   |
| `/api/ai/research/current`                | DELETE | 软删除当前可见研究记录（待审核+已完成） |

这些接口均等待模型或研究流程结束后一次性返回 JSON。普通对话不会搜索新闻；深度研究先搜站内再补站外，并生成带来源编号的 Markdown 报告。字段级说明、请求示例和审核动作见 `API接口规范文档.md`。

## 1-8 认证机制

系统使用基于令牌(Token)的认证机制：

1. 用户注册或登录成功后返回访问令牌
2. 需要认证的接口在请求头中添加 `Authorization: Bearer <token>`
3. 令牌有效期为 7 天
4. 编辑/删除新闻接口在认证之外还需进行归属校验：文章不存在返回 404，
   非本人文章（含无主文章）返回 403

需要认证的范围：

- 用户信息、资料修改和密码修改
- 新闻发布、我的发布、新闻编辑和删除
- 全部收藏与浏览历史接口
- 全部 AI 接口（对话、自动模式、研究启动/审核/恢复/清空）

新闻公开查询（分类、列表、搜索、详情）暂不要求登录。缺少 `Authorization` 请求头时，受保护接口由 FastAPI 返回 HTTP 422；token 无效或过期返回 HTTP 401。审核他人的 `thread_id` 返回 HTTP 403；对非待审核任务提交审核返回 HTTP 409。

## 1-9 错误处理

系统提供统一的错误处理机制：

- 用户认证失败返回 401 状态码
- 无权操作他人资源返回 403 状态码
- 资源不存在返回 404 状态码
- 研究任务不在待审核状态时提交审核返回 409 状态码
- 参数校验失败返回 422 状态码（FastAPI 默认 `detail` 数组，不走 `{code, message, data}` 包装）
- 服务器内部错误返回 500 状态码

业务类 `HTTPException` 通常仍使用 `{code, message, data}` 结构。当前处于开发模式，未捕获异常的 `data` 中可能包含异常详情和 traceback。LLM、结构化输出、搜索工具失败，以及无效或已丢失的研究 `thread_id`，当前也可能进入 HTTP 500。

## 1-10 开发规范

- 新闻、用户、收藏、历史使用异步数据库操作
- 所有密码均加密存储
- 业务接口返回统一的 JSON 格式 `{code, message, data}`
- 身份类字段（user_id、author）一律由服务端从 token 推导，请求体不接收
- 写操作显式 commit 后再执行缓存失效，保证时序正确
- 缓存操作容错处理：失败不中断业务，依赖 TTL 兜底
- AI 接口经 Service 层调用，Router 不直接操作 LangGraph 或搜索工具
- LLM 与搜索密钥只放在项目根目录 `.env` 中，不提交到版本控制
- 详细的接口文档和示例见 `API接口规范文档.md`

## 1-11 性能优化

- 使用 Redis 缓存热点数据，并通过写后失效保证一致性
- 新闻列表查询按 `publish_time DESC, id DESC` 双字段排序，保证分页稳定
- 异步数据库操作提升并发性能
- 合理的数据库索引设计（含 user_id 归属索引、publish_time 排序索引）
- 连接池管理减少连接开销
- 研究 Agent 先用确定性规则检查来源数量和媒体多样性，明显不足时跳过一次 LLM 证据审查

## 1-12 新闻研究 Agent

当前研究 Agent 的定位是：**外层由 FastAPI 固定接入 HTTP（需登录），内层由 LangGraph 执行规划、检索、评估和写稿，草稿必须经过用户审核后才能定稿。**

### 处理流程

```
analyze_goal → search_news → assess_evidence
                    ↑ 证据不足且未满 3 轮
                    └── search_again
证据充分或已达自动搜索上限 → generate_draft → human_review（interrupt）
审核结果：
  approve       → finalize（把当前草稿确认为最终报告，不重写正文）
  revise        → generate_draft（按修改意见生成完整替代草稿）
  research_more → search_news（用户主动补查，硬上限 5 轮）
  change_goal   → analyze_goal（清空旧资料，按新目标重新开始）
```

### 检索策略（站内 + 站外）

规划节点一次产出两类查询，避免把同一句话同时交给 Tavily 和 SQL LIKE：

- `current_query`：给站外 Tavily 的一句自然语言查询
- `internal_keywords`：给站内标题/简介 LIKE 的实体短词（如 `C919`），不含「发展」「历程」等抽象词

`search_news` 每轮最多取 5 条：先按 `internal_keywords` 逐词检索站内新闻（与首页搜索共用 `search_news_by_keyword`，按新闻 id 去重，URL 为 `internal://news/{id}`），不足再用 `current_query` 调用 Tavily 补齐。后续补搜只更新 `current_query`，站内继续复用第一轮关键词。规划漏给关键词时，会从用户原话中抽取 `C919` 这类型号作为兜底。

Tavily 当前使用 `topic=news`、`time_range=year`。此前锁死 `week` 时，「发展历程」类题目会只命中本周无关新闻；站内若把整句 `current_query` 拿去 LIKE，也匹配不到「C919国产大飞机…」这类标题。

### 关键约束

- 初始自动搜索最多 3 轮（`max_search_rounds`），用户补查后的绝对上限为 5 轮（`hard_max_search_rounds`）
- 证据最低门槛：至少 3 条有效来源、至少 2 家独立媒体；满足后再由 LLM 检查研究问题覆盖和来源冲突（当前仍不按主题相关性丢弃来源）
- 报告只能依据搜索摘要中的信息写作，并使用 `[S1]` 形式引用真实来源编号；引用来源目录只列出正文实际引用过的编号
- Checkpointer 使用 `SqliteSaver`（`data/research_checkpoints.db`），重启后仍可按 `thread_id` 恢复；MySQL `research_run` 负责用户归属和「当前任务」索引
- 同一用户同时只展示一份研究：新 `start` 会把旧的 `waiting_review` 标为 `abandoned`；`GET /current` 先待审核、再最近已完成
- 图运行在 `asyncio.to_thread` 中；站内搜索使用独立异步引擎，不复用 FastAPI 请求会话
- 自动模式的 Research 分支只把本次 `message` 作为研究目标，不读取对话历史
- 澄清（`clarify`）只返回问题，不在后端保存待澄清状态；前端需把用户补充拼回完整问题后再次调用 `/api/ai/auto`
- 收藏、浏览历史不作为研究上下文；Agent 读站内新闻也不写入浏览历史、不增加浏览量

### 运行依赖

调用 AI 接口前，需要在项目根目录 `.env` 中配置：

```dotenv
LLM_API_KEY=...
LLM_MODEL_ID=...
LLM_BASE_URL=...
TAVILY_API_KEY=...
```

前端为普通对话设置 60 秒超时，为自动模式、研究启动和审核设置 300 秒超时。
